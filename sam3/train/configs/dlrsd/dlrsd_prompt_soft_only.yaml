# @package _global_
defaults:
  - _self_

paths:
  bpe_path: "sam3/assets/bpe_simple_vocab_16e6.txt.gz"
  experiment_log_dir: "outputs/dlrsd_prompt_soft_only_1X3090"

  train_img_dir: "data/DLRSD_split/train/imgs"
  val_img_dir: "data/DLRSD_split/val/imgs"

  train_ann: "data/sam3_semantic_fixed/dlrsd/train.sam3.semantic.json"
  val_ann: "data/sam3_semantic_fixed/dlrsd/val.sam3.semantic.json"

  sam3_ckpt: "weights/sam3/sam3.pt"

dlrsd_train:
  num_images: 8

  train_transforms:
    - _target_: sam3.train.transforms.basic_for_api.ComposeAPI
      transforms:
        - _target_: sam3.train.transforms.filter_query_transforms.FlexibleFilterFindGetQueries
          query_filter:
            _target_: sam3.train.transforms.filter_query_transforms.FilterCrowds

        - _target_: sam3.train.transforms.segmentation.DecodeRle

        - _target_: sam3.train.transforms.basic_for_api.RandomResizeAPI
          sizes:
            _target_: sam3.train.transforms.basic.get_random_resize_scales
            size: ${scratch.resolution}
            min_size: 256
            rounded: false
          max_size:
            _target_: sam3.train.transforms.basic.get_random_resize_max_size
            size: ${scratch.resolution}
          square: true
          consistent_transform: ${scratch.consistent_transform}

        - _target_: sam3.train.transforms.basic_for_api.PadToSizeAPI
          size: ${scratch.resolution}
          consistent_transform: ${scratch.consistent_transform}

        - _target_: sam3.train.transforms.basic_for_api.ToTensorAPI

        - _target_: sam3.train.transforms.filter_query_transforms.FlexibleFilterFindGetQueries
          query_filter:
            _target_: sam3.train.transforms.filter_query_transforms.FilterEmptyTargets

        - _target_: sam3.train.transforms.basic_for_api.NormalizeAPI
          mean: ${scratch.train_norm_mean}
          std: ${scratch.train_norm_std}

  val_transforms:
    - _target_: sam3.train.transforms.basic_for_api.ComposeAPI
      transforms:
        - _target_: sam3.train.transforms.segmentation.DecodeRle

        - _target_: sam3.train.transforms.basic_for_api.RandomResizeAPI
          sizes: ${scratch.resolution}
          max_size:
            _target_: sam3.train.transforms.basic.get_random_resize_max_size
            size: ${scratch.resolution}
          square: true
          consistent_transform: false

        - _target_: sam3.train.transforms.basic_for_api.ToTensorAPI

        - _target_: sam3.train.transforms.basic_for_api.NormalizeAPI
          mean: ${scratch.val_norm_mean}
          std: ${scratch.val_norm_std}

  loss:
    _target_: sam3.train.loss.rs_semantic_loss.RSSemanticOnlyLoss

    loss_fn_semantic_seg:
      _target_: sam3.train.loss.loss_fns.SemanticSegCriterion
      presence_head: False
      presence_loss: False
      focal: True
      focal_alpha: 0.6
      focal_gamma: 2.0
      downsample: False
      weight_dict:
        loss_semantic_seg: 20.0
        loss_semantic_dice: 30.0

scratch:
  enable_segmentation: True
  use_presence_eval: True

  matcher:
    _target_: sam3.train.matcher.BinaryHungarianMatcherV2
    focal: true
    cost_class: 2.0
    cost_bbox: 2.0
    cost_giou: 1.0
    alpha: 0.25
    gamma: 2.0
    stable: False

  scale_by_find_batch_size: True

  resolution: 1008
  consistent_transform: False
  max_ann_per_img: 200

  train_norm_mean: [0.5, 0.5, 0.5]
  train_norm_std: [0.5, 0.5, 0.5]
  val_norm_mean: [0.5, 0.5, 0.5]
  val_norm_std: [0.5, 0.5, 0.5]

  # 关键修正：先彻底关闭多进程 dataloader，避免 NFS .nfs busy
  num_train_workers: 0
  num_val_workers: 0

  hybrid_repeats: 1
  context_length: 2
  gather_pred_via_filesys: false

  # 关键修正：lr 降低，避免 soft prompt-only 中后期 spike
  lr_transformer: 1.0e-4
  lr_vision_backbone: 0.0
  lr_language_backbone: 0.0
  lrd_vision_backbone: 1.0
  wd: 0.0

  scheduler_timescale: 20
  scheduler_warmup: 10
  scheduler_cooldown: 10

  gradient_accumulation_steps: 1

  # 保持 batch=16，当前是比较好的甜点区
  train_batch_size: 16
  val_batch_size: 16

  collate_fn:
    _target_: sam3.train.data.collator.collate_fn_api
    _partial_: true
    repeats: ${scratch.hybrid_repeats}
    dict_key: all
    with_seg_masks: ${scratch.enable_segmentation}

  collate_fn_val:
    _target_: sam3.train.data.collator.collate_fn_api
    _partial_: true
    repeats: ${scratch.hybrid_repeats}
    dict_key: all
    with_seg_masks: ${scratch.enable_segmentation}

trainer:
  _target_: sam3.train.trainer.Trainer
  skip_saving_ckpts: False
  empty_gpu_mem_cache_after_eval: True
  skip_first_val: False
  max_epochs: 10
  accelerator: cuda
  seed_value: 1230
  val_epoch_freq: 1
  mode: train
  gradient_accumulation_steps: ${scratch.gradient_accumulation_steps}

  distributed:
    backend: nccl
    find_unused_parameters: True
    gradient_as_bucket_view: True

  loss:
    all: ${dlrsd_train.loss}
    default:
      _target_: sam3.train.loss.sam3_loss.DummyLoss

  data:
    train:
      _target_: sam3.train.data.torch_dataset.TorchDataset
      dataset:
        _target_: sam3.train.data.rs_single_query_dataset.RSSingleQueryDataset
        img_folder: ${paths.train_img_dir}
        ann_file: ${paths.train_ann}
        transforms: ${dlrsd_train.train_transforms}

      shuffle: True
      batch_size: ${scratch.train_batch_size}
      num_workers: ${scratch.num_train_workers}
      pin_memory: True
      drop_last: True
      collate_fn: ${scratch.collate_fn}

    val:
      _target_: sam3.train.data.torch_dataset.TorchDataset
      dataset:
        _target_: sam3.train.data.rs_single_query_dataset.RSSingleQueryDataset
        transforms: ${dlrsd_train.val_transforms}
        img_folder: ${paths.val_img_dir}
        ann_file: ${paths.val_ann}

      shuffle: False
      batch_size: ${scratch.val_batch_size}
      num_workers: ${scratch.num_val_workers}
      pin_memory: True
      drop_last: False
      collate_fn: ${scratch.collate_fn_val}

  model:
    _target_: sam3.model_builder.build_rs_prompt_semantic_model
    bpe_path: ${paths.bpe_path}
    device: cpu
    eval_mode: false
    enable_segmentation: ${scratch.enable_segmentation}
    checkpoint_path: ${paths.sam3_ckpt}
    load_from_HF: false

  meters: null

  optim:
    amp:
      enabled: True
      amp_dtype: bfloat16

    optimizer:
      _target_: torch.optim.AdamW

    gradient_clip:
      _target_: sam3.train.optim.optimizer.GradientClipper
      max_norm: 0.1
      norm_type: 2

    param_group_modifiers: null

    options:
      lr:
        - scheduler:
            _target_: sam3.train.optim.schedulers.InverseSquareRootParamScheduler
            base_lr: 3.0e-4
            timescale: ${scratch.scheduler_timescale}
            warmup_steps: ${scratch.scheduler_warmup}
            cooldown_steps: ${scratch.scheduler_cooldown}

      weight_decay:
        - scheduler:
            _target_: fvcore.common.param_scheduler.ConstantParamScheduler
            value: 0.0

  checkpoint:
    save_dir: ${paths.experiment_log_dir}/checkpoints
    save_freq: 1

  logging:
    tensorboard_writer:
      _target_: sam3.train.utils.logger.make_tensorboard_logger
      log_dir: ${paths.experiment_log_dir}/tensorboard
      flush_secs: 10
      should_log: True
    wandb_writer: null
    log_dir: ${paths.experiment_log_dir}/logs
    log_freq: 10
    log_scalar_frequency: 10
    log_visual_frequency: 100

launcher:
  num_nodes: 1
  gpus_per_node: 1
  experiment_log_dir: ${paths.experiment_log_dir}
  multiprocessing_context: forkserver

submitit:
  account: null
  partition: null
  qos: null
  timeout_hour: 72
  use_cluster: False
  cpus_per_task: 8
  port_range: [10000, 65000]